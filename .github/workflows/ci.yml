name: CI

on:
  push:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: publish-main
  cancel-in-progress: true

jobs:
  publish:
    name: Publish VSIX to VSCE and Open VSX
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    env:
      VSCE_PAT: ${{ secrets.VSCE_PAT }}
      OVSX_PAT: ${{ secrets.OVSX_PAT }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Set version from run number (no git tag)
        run: |
          : ${GITHUB_RUN_NUMBER:?GITHUB_RUN_NUMBER is not set}
          # Compute major.minor.<run_number> robustly via Node
          VERSION=$(node -p 'const v=require("./package.json").version||"0.0.0"; const a=String(v).split("."); const M=a[0]||"0"; const m=a[1]||"0"; `${M}.${m}.${process.env.GITHUB_RUN_NUMBER}`')
          # Write version without creating git tags or commits
          npm pkg set version="$VERSION"
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Package extension (.vsix)
        run: npx vsce package

      - name: Locate VSIX
        id: vsix
        run: |
          VSIX_FILE=$(ls *.vsix)
          echo "vsix=${VSIX_FILE}" >> $GITHUB_OUTPUT

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ env.VERSION }}
          name: v${{ env.VERSION }}
          target_commitish: ${{ github.sha }}
          generate_release_notes: true
          files: ${{ steps.vsix.outputs.vsix }}

      - name: Publish to VS Code Marketplace (VSCE)
        if: env.VSCE_PAT != ''
        run: npx vsce publish --packagePath "${{ steps.vsix.outputs.vsix }}" -p "$VSCE_PAT"

      - name: Publish to Open VSX Registry (OVSX)
        if: env.OVSX_PAT != ''
        run: npx ovsx publish "${{ steps.vsix.outputs.vsix }}" -p "$OVSX_PAT"

      - name: Upload VSIX artifact
        uses: actions/upload-artifact@v4
        with:
          name: extension-${{ env.VERSION }}
          path: ${{ steps.vsix.outputs.vsix }}
